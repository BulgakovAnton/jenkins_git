#!groovy

properties([disableConcurrentBuilds()])

pipeline {
    agent{
        label 'master'
        }
    options {
        buildDiscarder(logRotaator(numToKeepStr: '10', artifactNumToKeepStr : '10' ))
        timestamp()
        }
    }
    